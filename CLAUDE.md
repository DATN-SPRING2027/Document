# DATN Claude workspace instructions

Đọc và tuân thủ toàn bộ [`AI_WORKFLOW.md`](AI_WORKFLOW.md) và [`AGENTS.md`](AGENTS.md)
trước khi chỉnh sửa. Quy tắc bắt buộc gồm: mỗi task dùng branch mới từ
`origin/main`; DB/BE/FE tách branch và PR; không sửa migration cũ; AI được
push feature branch đã kiểm tra/commit nhưng không push trực tiếp lên `main`,
approve hoặc merge; feature PR chỉ vào `main`; link PR được cập nhật
vào Jira; database change phải báo team; mọi thay đổi phải theo architecture,
được kiểm tra và có báo cáo bàn giao.
